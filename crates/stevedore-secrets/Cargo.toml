[package]
name = "stevedore-secrets"
description = "Move secrets between password managers and vaults — the core library."
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["secrets", "password-manager", "vault", "migration"]
categories = ["api-bindings", "cryptography"]

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# The live tests read an item back through the store's CLI to check what landed.
serde_json = { workspace = true }

[lints]
workspace = true
