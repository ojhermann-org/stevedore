[package]
name = "stevedore-secrets-mcp"
description = "MCP server exposing stevedore's secret-migration tools (scaffold)."
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["secrets", "password-manager", "vault", "migration", "mcp"]
categories = ["command-line-utilities", "cryptography"]

[[bin]]
name = "stevedore-mcp"
path = "src/main.rs"

# No dependencies yet — the tool surface is deferred.

[lints]
workspace = true
